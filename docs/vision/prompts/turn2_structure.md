# Turn 2 — Structure critique

**Don't hand-copy the schema from source anymore.** Run:

```bash
botdraw vision brief <job_id> --turn 2
```

Writes the exact prompt + images + save-the-reply instructions to a
folder (see `turn1_scene.md` for the general flow).

Notes specific to this turn: check silhouette, band/parallel gaps, AND
short interior accent strokes (e.g. a mug's lower body line). Prefer
`keep_more_edges` / `equalize_structure` fixes.
